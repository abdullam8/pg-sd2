extends layout
block content
    .main-content
    h1 All students
    table(border=1)
        for item in data
            tr
                td #{item.id}
                td 
                    a(href='/single-student/' + item.id) #{item.name}