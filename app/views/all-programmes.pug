extends layout
block content
    .main-content
    h1 All Programmes
    table(border=1)
        for item in data
            tr
                td #{item.id}
                td 
                    a(href='/programme/' + item.id) #{item.name}